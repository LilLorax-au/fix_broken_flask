# Project 1_3 Files

This is a flask app that was broken, the project is to make it funcional

## Requirements
Python 3.14

## Setup

1. Create virtual environment:  \
    `python -m venv .venv`
2. Activate virtual environment: \
    (for Powershell) `.\venv\Scripts\Activate.ps1` \
    If error is raised, run the following, then try again: \
    `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
3. Install dependencies: \
    `pip install requirements.txt`

## Alt Setup
1. Run PowerShell script: \
    while project_1 is at root run `.\install.ps1` \
    If error is raised, run the following, then try again: \
    `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`

[THIS TEMPLATE WAS CREATED BY: ISAAC M.D. BROWN]

